import reusableColors from './reusableColors';

const tabBarActiveTextColor = '#56A8F7';

const tabBarUnderlineStyle = {
  height: 2,
  backgroundColor: '#56A8F7',
};

const contentProps = {
  style: {
    backgroundColor: reusableColors.detailScreenBgColor,
    flex: 1,
  },
};
export default { tabBarActiveTextColor, tabBarUnderlineStyle, contentProps };
